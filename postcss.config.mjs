// This standalone site uses plain CSS (no Tailwind). An explicit empty config
// stops Next.js from walking up and inheriting the parent repo's PostCSS /
// Tailwind pipeline, which is unrelated to this site.
const config = {
  plugins: {},
};

export default config;
