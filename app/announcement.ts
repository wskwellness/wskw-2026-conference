// ─────────────────────────────────────────────────────────────────────────
//  EDIT THIS FILE TO CONTROL THE ANNOUNCEMENT BANNER AT THE TOP OF THE SITE.
//
//  • Leave `message` as an empty string ("") to hide the banner completely.
//  • Put any live update in `message` (e.g. a room change during the event).
//  • `linkLabel` / `linkHref` are optional — leave both "" for no link.
//
//  Changing the message re-shows the banner even for people who dismissed
//  the previous one, so each new announcement is seen.
// ─────────────────────────────────────────────────────────────────────────
export const announcement = {
  message: "",
  linkLabel: "",
  linkHref: "",
};
